what is angularjs?
scope, root scope,
controller, view , model,
directive,
filter,
constant,
values,
service, custom service
factory,
provider,
config,
run,
route, 
state, 
two way binding - one way binding,
watch, 
watchcollection,
watchgroup, 
apply,
digest cycle,
eval,parse,interpolate,
transclusion,
ngRoute, stateprovider,
resolve,
broadcast, emit, custome events
bootstrap process, 
route and state diff,
ng-if and ng-show diff,
angularjs 1 and 2 diff